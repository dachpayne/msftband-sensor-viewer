<Page
    x:Class="Band.Samples.Views.ConnectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Band.Samples.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Connect your Band" Style="{ThemeResource HeaderTextBlockStyle}"/>
            <TextBlock Text="Enter your text here."/>
        </StackPanel>

        
        <ListView Header="Discovered Bands" Grid.Row="1">
            <ListView.Items>
                <StackPanel>
                    <TextBlock Text="David's Band"/>
                    <TextBlock Text="aa:aa:aa:aa:aa" Opacity="0.8"/>
                </StackPanel>

            </ListView.Items>
        </ListView>
        
        
        <StackPanel Grid.Row="2">
            <Button Content="Connect to Band"/>
        </StackPanel>
       
    </Grid>
</Page>
